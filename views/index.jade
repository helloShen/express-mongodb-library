extends layout

block content
  h1.title= title
  hr
  //- h1 Dynamic content

  //- if error
  //-   p Error getting dynamic content.
  //- else
  //- p The library has the following record counts:
  p The library has the following record counts:

    ul
      li #[strong Books ]
        span.badge.bg-primary !{data.book_count}
      li #[strong Copies ]
        span.badge.bg-primary !{data.book_instance_count}
      li #[strong Copies available ]
        span.badge.bg-primary !{data.book_instance_available_count}
      li #[strong Authors ]
        span.badge.bg-primary !{data.author_count}
      li #[strong Genres ]
        span.badge.bg-primary !{data.genre_count}
  
  p Feel free to add your favorite books to our library.